package:
  name: torchtrainer
  version: "1.2"

source:
  path: ../

build:
  number: 1

test:
  requires:
    - python

requirements:
  host:
    - python

  run:
    - python

about:
  home: git@github.com:francescocastelli/torch-trainer.git
